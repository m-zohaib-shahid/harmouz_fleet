# AegisFleet backend - 100% free / open source stack, no cloud dependencies required at runtime.
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1

# Test-time only (pip install -r requirements-dev.txt)
